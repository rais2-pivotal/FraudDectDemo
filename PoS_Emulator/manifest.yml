---
applications:
- name: pos_emulator
  memory: 512M 
  instances: 1
  host: pos_emulator
  path: build/libs/PoS_Emulator.jar
  no-route: true
  env:
    skipSetup: true

# cf push --no-start   
# cf set-health-check pos_emulator none
# cf start pos_emulator    